# AWS 内存估算启发式

本技能根据实例 family 给出近似内存比（`memory_gb = vcpu * ratio`）：

- `r/x/u/z`（内存型）→ `ratio = 8.0`
- `m/i/d`（通用/存储倾向）→ `ratio = 4.0`
- `c/t`（计算型/突发型）→ `ratio = 2.0`
- `p/g`（GPU）→ `ratio = 8.0`
- 其他 family → `ratio = 4.0`

## 说明
- 这是迁移与报价前置阶段的快速估算，不是精确硬件规格。
- 若需要精确规格，应额外接入官方实例规格数据源。
